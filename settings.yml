show_likes:
  type: bool
  default: true
show_views:
  type: bool
  default: false
show_reply_count:
  type: bool
  default: false
show_activity:
  type: bool
  default: false
show_publish_date:
  type: bool
  default: true
set_card_max_height:
  type: bool
  default: true
card_max_height:
  type: integer
  default: 275
list_view_categories:
  type: list
  list_type: category
  default: ""
  description: "Categories that display topic cards in list style (image left, content right). If both settings are empty, topic cards are shown everywhere in list style by default."
grid_view_categories:
  type: list
  list_type: category
  default: ""
  description: "Categories that display topic cards in grid style (image top, content below in responsive grid). If a category appears in both settings, grid style takes priority."
show_for_suggested_topics:
  type: bool
  default: true
card_style_mobile:
  type: enum
  default: grid
  choices:
    - list
    - grid
  description: "Choose card layout style for mobile viewports. This applies to all categories where topic cards are enabled."
grid_card_min_width:
  type: integer
  default: 260
  description: "Desktop/tablet grid: minimum column width (px). Grid auto-fits as many cards as fit per row. No effect on smartphones."
grid_thumbnail_max_height:
  type: integer
  default: 150
  description: "Smartphone grid only: maximum thumbnail height in px. Desktop grid unaffected."

set_card_grid_height:
  type: bool
  default: true
card_grid_height:
  type: integer
  default: 420

thumbnail_placeholder_icon:
  type: enum
  default: fa-image
  choices:
    - fa-image
    - fa-file
    - fa-newspaper
  description: "Icon to display when a topic has no thumbnail image (Font Awesome icon name, e.g., fa-image)."

card_border_radius:
  type: enum
  default: large
  choices:
    - none
    - small
    - medium
    - large
    - extra_large

hide_topic_author_tc_column:
  type: bool
  default: false
  description: "When enabled, hides td.custom-author-column in topic cards (for compatibility with Topic Author TC)."

# Topic title font size for cards
topic_title_font_size:
  type: enum
  default: "var(--font-0)"
  choices:
    - "var(--font-down-1)"
    - "var(--font-0)"
    - "var(--font-up-1)"
    - "var(--font-up-2)"
    - "var(--font-up-3)"
  description: "Font size for topic titles in topic cards. Uses Discourse's built-in font-size CSS variables."